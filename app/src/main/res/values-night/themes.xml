<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de la aplicación para el modo oscuro -->
    <style name="Base.Theme.LaboratorioCalificado03" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Personalización de colores para el modo oscuro -->
        <item name="colorPrimary">@color/primaryDark</item>
        <item name="colorPrimaryContainer">@color/primaryContainerDark</item>
        <item name="colorSecondary">@color/secondaryDark</item>
        <item name="colorSecondaryContainer">@color/secondaryContainerDark</item>
        <item name="colorSurface">@color/darkBackground</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorOnSurface">@color/lightText</item>
        <item name="colorOnBackground">@color/lightText</item>
        <item name="android:textColor">@color/lightText</item>
    </style>

    <!-- Define el tema de la aplicación -->
    <style name="Theme.LaboratorioCalificado03" parent="Base.Theme.LaboratorioCalificado03">
        <!-- Otros atributos personalizados -->
    </style>
</resources>
